const subjectParser = require('./subjectParser');
const problemGroupParser = require('./problemGroupParser');
const quizParser = require('./quizParser');

module.exports = {
    subjectParser,
    problemGroupParser,
    quizParser
}